/**
 * Expo reading-session store facade.
 * Keep the Expo import path stable while sharing the core implementation.
 */

export { useReadingSessionStore } from "@readany/core/stores";
export type { ReadingSessionState } from "@readany/core/stores";
