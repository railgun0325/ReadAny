/**
 * Expo annotation store facade.
 * Keep the Expo import path stable while sharing the core implementation.
 */

export { useAnnotationStore } from "@readany/core/stores";
export type { AnnotationState, HighlightStats } from "@readany/core/stores";
